#pragma once
#include <bits/stdc++.h>
#include <chrono>
#include <thread>
using namespace std;
struct action {
    string state;
    string exp_symbol;
    string new_symb;
    string move;
    string next_state;
};
